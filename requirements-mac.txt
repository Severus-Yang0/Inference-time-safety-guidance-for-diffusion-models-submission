# Mac (M2) — MPS backend. Install inside a conda env.
# torch is installed from PyPI; MPS is available on macOS 12.3+ with PyTorch >= 2.1.
torch>=2.1
torchvision>=0.16
diffusers>=0.27
transformers>=4.40
accelerate>=0.30
safetensors>=0.4
huggingface_hub>=0.23
pillow
numpy
pyyaml
tqdm
pandas
open_clip_torch>=2.24
