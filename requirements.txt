# Install torch first from the cu124 index (cu121 wheels lack sm_90 kernels on recent torch):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
torch>=2.1
torchvision>=0.16
diffusers>=0.27
transformers>=4.40
accelerate>=0.30
safetensors>=0.4
huggingface_hub>=0.23
pillow
numpy
pyyaml
tqdm
pandas
open_clip_torch>=2.24
datasets>=2.18        # for prompt-building (UnsafeBench etc. via HF)
clean-fid>=0.1.35     # FID on neutral subset (scripts/compute_fid.py)
nudenet>=3.0          # disjoint cross-evaluator on explicit stratum
onnxruntime>=1.17     # backend for nudenet ONNX model
matplotlib>=3.7       # plotting (Pareto, galleries, distributions)
scipy>=1.10           # KDE for score-distribution figure
